<nav class="app-header navbar navbar-expand bg-body-secondary">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" data-lte-toggle="sidebar" href="#" role="button">
                    <i class="bi bi-list"></i>
                </a>
            </li>
            <li class="nav-item d-none d-md-block"><a href="~/" class="nav-link">Trang chủ</a></li>            
        </ul>        
        <ul class="navbar-nav ms-auto">            
    @{
        var userData = User.GetUserData();
        // Kiểm tra nếu không có ảnh thì dùng ảnh mặc định nophoto.png
        var userPhoto = string.IsNullOrEmpty(userData?.Photo) ? "nophoto.png" : userData.Photo;
    }

    <li class="nav-item dropdown user-menu">
        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
            @* 1. Ảnh nhỏ trên thanh Navbar *@
            <img src="~/images/employees/@userPhoto" 
                 class="user-image rounded-circle shadow" 
                 alt="User Image"
                 onerror="this.onerror=null;this.src='/images/employees/nophoto.png';" />
            
            <span class="d-none d-md-inline">@userData?.DisplayName</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-end">                    
            <li class="user-header text-bg-primary">
                @* 2. Ảnh lớn trong Menu thả xuống *@
                <img src="~/images/employees/@userPhoto" 
                     class="rounded-circle shadow" 
                     alt="User Image"
                     onerror="this.onerror=null;this.src='/images/employees/nophoto.png';" />
                <p>
                    @userData?.DisplayName
                    <small>@userData?.UserName</small>
                </p>
            </li>                    
            <li class="user-footer">
                <a href="~/Account/ChangePassword" class="btn btn-default btn-flat">Đổi mật khẩu</a>
                <a href="~/Account/Logout" class="btn btn-default btn-flat float-end">Thoát</a>
            </li>                    
        </ul>
    </li>            
</ul> 
    </div>
</nav>